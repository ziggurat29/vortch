// Generated by CMake (vortch_embed_assets) from cmake/embedded_assets.cpp.in.
// Do not edit — regenerated at configure time from the files under assets/.
#include "embedded_assets.hpp"

#include <cstring>

namespace vortch {
namespace {

@VORTCH_EMBED_ARRAYS@
struct Entry {
  const char*          name;
  const unsigned char* data;
  std::size_t          size;
};

const Entry kEntries[] = {
@VORTCH_EMBED_ENTRIES@};

}  // namespace

EmbeddedAsset embedded_asset(const char* name) {
  for (const Entry& e : kEntries) {
    if (std::strcmp(e.name, name) == 0) return { e.data, e.size };
  }
  return { nullptr, 0 };
}

}  // namespace vortch
