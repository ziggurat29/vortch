#pragma once
// Generated by CMake (vortch_embed_assets) from cmake/embedded_assets.hpp.in.
// Do not edit — regenerated at configure time from the files under assets/.
#include <cstddef>

namespace vortch {

// A blob of asset bytes embedded in the binary. Stored verbatim, so this is
// format-agnostic: SVG today, raster (PNG/etc.) later without changes here.
struct EmbeddedAsset {
  const unsigned char* data;  // nullptr if the requested name is unknown
  std::size_t          size;
};

// Look up an embedded asset by its path relative to assets/ (e.g. "icon.svg",
// "overlays/done.svg"). Returns { nullptr, 0 } if not found.
EmbeddedAsset embedded_asset(const char* name);

}  // namespace vortch
